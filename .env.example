# Frontend uses the Vite dev proxy by default. For a deployed frontend,
# configure the backend URL at the hosting layer or replace the API base.
VITE_API_BASE=

# Backend settings live in backend/.env.example.
